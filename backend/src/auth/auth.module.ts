import { Module } from '@nestjs/common';

/**
 * Authentication Module
 * Prepared for future integration of Passport, JWT, Refresh tokens, and OAuth providers
 * (Google, Apple, Microsoft, Facebook).
 */
@Module({
  imports: [],
  controllers: [],
  providers: [],
  exports: [],
})
export class AuthModule {}
